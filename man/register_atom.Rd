% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/atoms.R
\name{register_atom}
\alias{register_atom}
\title{Register a primitive atom in the \code{mmad_expr} framework}
\usage{
register_atom(name, arity, value, grad, hess, dcp_info = NULL)
}
\arguments{
\item{name}{Atom name (a single character string).}

\item{arity}{Integer arity, or \code{NA_integer_} for variadic atoms.}

\item{value, grad, hess}{Functions computing value, gradient, and Hessian
with respect to the atom's \emph{own arguments} (not theta). Each takes
\verb{(args, params)}, where \code{args} is a numeric vector of argument values
and \code{params} is the atom's parameter list. \code{value()} returns a scalar,
\code{grad()} returns a numeric vector of length \code{length(args)}, and
\code{hess()} returns a \verb{length(args) x length(args)} numeric matrix.}

\item{dcp_info}{Function \verb{(args_sign, params)} returning a list with
components \code{curvature} (one of \code{"affine"}, \code{"convex"}, \code{"concave"},
\code{"unknown"}), \code{monotonicity} (a character vector of length equal to
the arity, with entries \code{"nondecreasing"}, \code{"nonincreasing"}, or
\code{"unknown"}), and \code{sign} (one of \code{"positive"}, \code{"nonneg"},
\code{"negative"}, \code{"nonpos"}, \code{"zero"}, \code{"unknown"}). Used by
\code{\link[=curvature]{curvature()}} and the Phase 3 minorization engine.}
}
\value{
Invisibly \code{NULL}. Called for its side effect on the registry.
}
\description{
Register a primitive atom in the \code{mmad_expr} framework
}
\examples{
\dontrun{
# Register a custom 'tanh' atom (advanced; rarely needed):
register_atom("tanh",
  arity = 1L,
  value = function(args, params) tanh(args[1]),
  grad  = function(args, params) 1 - tanh(args[1])^2,
  hess  = function(args, params) matrix(-2 * tanh(args[1]) * (1 - tanh(args[1])^2), 1, 1))
}
}
\keyword{internal}
