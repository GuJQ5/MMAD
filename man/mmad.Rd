% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/mmad_driver.R
\name{mmad}
\alias{mmad}
\title{Minorization-Maximization driver for an \code{mmad_expr} target}
\usage{
mmad(
  expr,
  init,
  data = NULL,
  tol = 1e-06,
  max_iter = 1000,
  line_search_max = 30,
  track_history = FALSE,
  verbose = FALSE
)
}
\arguments{
\item{expr}{An \code{mmad_expr} or a one-sided formula
(\code{~ ...}). When a formula, it is lowered via \code{\link[=as_mmad_expr]{as_mmad_expr()}} using
\code{init} and \code{data} as the parameter vocabulary and data
environment.}

\item{init}{Numeric vector of initial parameter values.
May be named, in which case the names define the parameter
vocabulary (e.g. \code{init = c(alpha = 1, beta = 0.5)}) and the
returned estimate carries those names.}

\item{data}{Optional list / data frame of values for
theta-free symbols referenced in the formula. Default: the
formula's environment.}

\item{tol}{Convergence tolerance for both the maximum
absolute parameter change and the maximum absolute target gradient
component.}

\item{max_iter}{Maximum number of MM iterations (default 1000).}

\item{line_search_max}{Maximum number of step-halving steps per
iteration's line search (default 30).}

\item{track_history}{If \code{TRUE}, return a data frame of per-iteration
diagnostics (iteration index, value, gradient norm, parameter
change). Default \code{FALSE}.}

\item{verbose}{If \code{TRUE}, print per-iteration diagnostics.}
}
\value{
A list with components
\describe{
\item{\code{estimate}}{the final parameter vector (named if \code{init} was named)}
\item{\code{value}}{the target value at \code{estimate}}
\item{\code{iterations}}{number of iterations actually run}
\item{\code{converged}}{\code{TRUE} iff both convergence criteria were met}
\item{\code{history}}{per-iteration diagnostics (data frame), or \code{NULL}}
\item{\code{message}}{character: short status string}
}
}
\description{
Given a target expression \code{expr} and an initial parameter vector
\code{init}, this iteratively maximizes \code{expr} by constructing a
separable surrogate at the current iterate (via \code{\link[=minorize_at]{minorize_at()}}),
taking Newton steps on the separable and entangled blocks, and
line-searching on the target to enforce ascent.
}
\examples{
fit <- mmad(~ log(theta[1] + theta[2]) - theta[1] - theta[2] + 2,
            init = c(2, 2))
fit$estimate
\donttest{
# Poisson regression via sum() and X \%*\% theta:
set.seed(1); n <- 50; p <- 2
X <- matrix(rnorm(n * p, sd = 0.5), nrow = n)
y <- rpois(n, exp(X \%*\% c(0.3, -0.2)))
mmad(~ sum(y * (X \%*\% theta) - exp(X \%*\% theta)),
     init = rep(0, p), data = list(X = X, y = y))
}
}
