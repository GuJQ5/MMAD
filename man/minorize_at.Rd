% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/minorize.R
\name{minorize_at}
\alias{minorize_at}
\title{Build a surrogate of an \code{mmad_expr} at a current iterate}
\usage{
minorize_at(expr, theta_0)
}
\arguments{
\item{expr}{An \code{mmad_expr}.}

\item{theta_0}{A numeric vector giving the current iterate.}
}
\value{
A list with components
\describe{
\item{\code{expr}}{the full surrogate as an \code{mmad_expr}}
\item{\code{constant}}{the additive constant of the surrogate}
\item{\code{per_coord}}{a list of length \code{length(theta_0)} whose
\code{j}-th entry is the 1-d surrogate term in \code{theta[j]}, or
\code{NULL} if no term touches that coordinate}
\item{\code{non_separable}}{an \code{mmad_expr} representing the
multi-coordinate residue we could not minorize, or \code{NULL} if
every leaf was minorized or extracted}
\item{\code{is_separable}}{\code{TRUE} iff \code{non_separable} is \code{NULL}}
}
}
\description{
Given a target \code{expr} and a current iterate \code{theta_0}, construct a
surrogate \code{S(theta | theta_0)} that lower-bounds \code{expr(theta)} and is
tangent to it at \code{theta_0}. The surrogate is constructed to be as
separable across the coordinates of theta as the structure permits;
anything that resists clean minorization is preserved as-is in a
fallback bucket so that value/gradient/Hessian computations on the
full surrogate remain available.
}
\details{
Construction uses only Jensen's inequality and the supporting
hyperplane; no per-atom tight surrogates are applied.
}
\examples{
expr <- log(0.5 * mmad_var(1) + 0.5 * mmad_var(2))
surr <- minorize_at(expr, c(2, 1))
surr$is_separable
evaluate_expr(surr$expr, c(2, 1))$value     # tangent at theta_0
}
