% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/evaluate_expr.R
\name{evaluate_expr}
\alias{evaluate_expr}
\title{Evaluate an \code{mmad_expr} at a parameter vector}
\usage{
evaluate_expr(expr, theta)
}
\arguments{
\item{expr}{An object of class \code{mmad_expr}.}

\item{theta}{A numeric vector of parameter values.}
}
\value{
A list with components \code{value}, \code{gradient}, and \code{hessian}.
}
\description{
Computes the value, gradient, and Hessian of the expression at \code{theta},
all with respect to theta. The gradient is a numeric vector of length
\code{length(theta)}; the Hessian is a \code{length(theta)} by \code{length(theta)}
numeric matrix.
}
\details{
This is the Phase 1 replacement for \code{Function_evaluation()} -- it works
on the new expression tree. The legacy function continues to work on
the old list representation; \code{legacy_to_expr()} bridges between them.
}
\examples{
expr <- log(mmad_var(1) + mmad_var(2))
evaluate_expr(expr, c(1, 1))
}
