% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/mmad_driver.R
\name{mmad_test}
\alias{mmad_test}
\title{Wald significance tests for the parameters of an \code{mmad_fit} object}
\usage{
mmad_test(object, ...)
}
\arguments{
\item{object}{An \code{mmad_fit} object returned by \code{\link[=mmad]{mmad()}}.}

\item{...}{Ignored.}
}
\value{
A matrix of class \code{"mmad_test"} with one row per parameter
and columns \code{Estimate}, \verb{Std. Error}, \verb{z value} and \verb{Pr(>|z|)}.
Its print method displays the table with significance stars via
\code{\link[stats:printCoefmat]{stats::printCoefmat()}}.
}
\description{
Performs a Wald z-test of the null hypothesis \eqn{H_0: \theta_j = 0}
for each parameter of a fit returned by \code{\link[=mmad]{mmad()}}. The test statistic
is the parameter estimate divided by its asymptotic standard error
(the square root of the corresponding diagonal element of
\link[=mmad_fit-methods]{vcov()}), and the two-sided p-value is computed
from the standard normal distribution. The tests are statistically
meaningful when the target function is a log-likelihood.
}
\examples{
fit <- mmad(~ 12 * log((theta[1] + theta[2] + 1) / 2) +
              15 * log((2 * theta[1] + theta[2]) / 3) +
              9  * log((theta[1] + 2 * theta[2]) / 3) -
              6 * theta[1] - 6 * theta[2],
            init = c(4, 2))
mmad_test(fit)
}
