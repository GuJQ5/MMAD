% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/mmad_driver.R
\name{mmad_fit-methods}
\alias{mmad_fit-methods}
\alias{coef.mmad_fit}
\alias{vcov.mmad_fit}
\alias{logLik.mmad_fit}
\alias{confint.mmad_fit}
\title{Extract information from an \code{mmad_fit} object}
\usage{
\method{coef}{mmad_fit}(object, ...)

\method{vcov}{mmad_fit}(object, ...)

\method{logLik}{mmad_fit}(object, ...)

\method{confint}{mmad_fit}(object, parm, level = 0.95, ...)
}
\arguments{
\item{object}{An \code{mmad_fit} object returned by \code{\link[=mmad]{mmad()}}.}

\item{...}{Ignored.}

\item{parm}{A specification of which parameters are to be given
confidence intervals: a vector of numbers or a vector of names.
If missing, all parameters are considered.}

\item{level}{The confidence level required (default \code{0.95}).}
}
\value{
\code{coef()}: a (possibly named) numeric vector of parameter
estimates. \code{vcov()}: a numeric matrix, the inverse of the negative
Hessian of the target function at the estimates. \code{logLik()}: an
object of class \code{"logLik"} whose \code{df} attribute is the number of
parameters. \code{confint()}: a matrix with one row per parameter and
columns giving the lower and upper confidence limits at the
requested \code{level}.
}
\description{
Standard accessor methods for the fitted objects returned by \code{\link[=mmad]{mmad()}},
in the spirit of the methods provided for \code{MASS::fitdistr()}:
\code{coef()} returns the parameter vector at the optimum, \code{vcov()} returns
the inverse of the negative Hessian of the target function at the
optimum, and \code{logLik()} returns the final target value as a
\code{"logLik"} object.
}
\details{
\code{vcov()}, \code{logLik()} and \code{confint()} are statistically meaningful
when the target function is a log-likelihood. In that case
\code{sqrt(diag(vcov(object)))} gives the asymptotic standard errors of
the parameter estimates, and \code{confint(object, level = 0.95)} gives
the corresponding Wald confidence intervals at any user-specified
confidence level. See also \code{\link[=mmad_test]{mmad_test()}} for Wald significance
tests of the individual parameters.
}
\examples{
fit <- mmad(~ 12 * log((theta[1] + theta[2] + 1) / 2) +
              15 * log((2 * theta[1] + theta[2]) / 3) +
              9  * log((theta[1] + 2 * theta[2]) / 3) -
              6 * theta[1] - 6 * theta[2],
            init = c(4, 2))
coef(fit)
vcov(fit)
sqrt(diag(vcov(fit)))   # asymptotic standard errors
logLik(fit)
confint(fit)
confint(fit, level = 0.90)
}
